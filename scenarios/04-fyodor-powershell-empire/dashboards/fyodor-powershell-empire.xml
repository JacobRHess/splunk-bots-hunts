<form>
  <label>FYODOR-L PowerShell implant</label>
  <description>Empire-style PowerShell foothold on FYODOR-L. Sysmon process-create view of encoded PowerShell, registry-backed payload, and scheduled-task persistence.</description>

  <fieldset submitButton="false">
    <input type="time" token="earliest_tok">
      <default>
        <earliest>-10y</earliest>
        <latest>now</latest>
      </default>
    </input>
  </fieldset>

  <row>
    <panel>
      <title>Encoded in-memory PowerShell events</title>
      <single>
        <search>
          <query>index=* sourcetype=XmlWinEventLog:Microsoft-Windows-Sysmon/Operational EventCode=1 Image=*powershell.exe (CommandLine=*FromBase64String* OR CommandLine=*-enc*) | stats count</query>
          <earliest>$earliest_tok.earliest$</earliest>
          <latest>$earliest_tok.latest$</latest>
        </search>
        <option name="colorBy">value</option>
        <option name="rangeColors">["0x53a051","0xdc4e41"]</option>
        <option name="rangeValues">[1]</option>
        <option name="useColors">1</option>
      </single>
    </panel>
  </row>

  <row>
    <panel>
      <title>Encoded PowerShell (FromBase64String / -enc) by host</title>
      <chart>
        <search>
          <query>index=* sourcetype=XmlWinEventLog:Microsoft-Windows-Sysmon/Operational EventCode=1 (CommandLine=*FromBase64String* OR CommandLine=*-enc*) | stats count by Computer | sort -count</query>
          <earliest>$earliest_tok.earliest$</earliest>
          <latest>$earliest_tok.latest$</latest>
        </search>
        <option name="charting.chart">bar</option>
      </chart>
    </panel>
  </row>

  <row>
    <panel>
      <title>FYODOR-L encoded PowerShell command lines</title>
      <table>
        <search>
          <query>index=* sourcetype=XmlWinEventLog:Microsoft-Windows-Sysmon/Operational EventCode=1 Computer=FYODOR-L.froth.ly Image=*powershell.exe (CommandLine=*FromBase64String* OR CommandLine=*-enc*) | stats count by User, CommandLine | sort -count</query>
          <earliest>$earliest_tok.earliest$</earliest>
          <latest>$earliest_tok.latest$</latest>
        </search>
      </table>
    </panel>
  </row>

  <row>
    <panel>
      <title>Persistence: PowerShell parented by svchost (scheduled task)</title>
      <table>
        <search>
          <query>index=* sourcetype=XmlWinEventLog:Microsoft-Windows-Sysmon/Operational EventCode=1 Computer=FYODOR-L.froth.ly CommandLine=*FromBase64String* | stats count by ParentImage, ParentCommandLine, User</query>
          <earliest>$earliest_tok.earliest$</earliest>
          <latest>$earliest_tok.latest$</latest>
        </search>
      </table>
    </panel>
  </row>
</form>
